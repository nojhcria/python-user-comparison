# python-user-comparison
Implemented a Python automation to compare user accounts exported from two environments using Pandas. The project identifies synchronization divergences and generates an Excel report with the results.
